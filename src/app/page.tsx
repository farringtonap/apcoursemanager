'use client';

import { Col, Container, Image, Row } from 'react-bootstrap';
import HomePage from './home/page';

/** The Home page. */
const Home = () => (
  <main>
    <HomePage />
  </main>
);

export default Home;
